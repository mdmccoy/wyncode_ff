<p id="notice"><%= notice %></p>

<!-- <h1>Pictures</h1> -->
<div class="container">
  <div class="row">
    <% @pictures.each do |picture| %>
        <div class="col-12 col-md-4 imghover" style= "display: relative;">
          <%= link_to image_tag(picture.url, class: 'img-fluid ' ), picture %>
          <div class="overlay">
            <div class="overlaytext">AVG rating + caption</div>
          </div>
      </div>
    <% end %>
  </div>
</div>

<%= link_to 'New Picture', new_picture_path %>
